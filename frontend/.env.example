# Leave unset for local dev (Vite proxy handles it — see vite.config.js).
# For production, set this to your deployed backend's URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
VITE_API_URL=
